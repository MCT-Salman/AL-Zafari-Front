# صفحة أمين المستودع (Warehouse Keeper)

## نظرة عامة
صفحة مخصصة لإدارة المستودع مع واجهة سهلة الاستخدام تتكون من قسمين رئيسيين: المدخلات ولوحة الأرقام مع جدول المخرجات.

## المتطلبات
- يمكن الوصول للصفحة فقط للمستخدمين الذين لديهم دور `Warehouse_Keeper`
- يتطلب اتصال بالإنترنت لجلب البيانات من الخادم

## الميزات

### 1. قسم المدخلات (الجانب الأيسر)
- **عرض الطلبات**: يعرض الطلبات المكتملة والجاهزة للإخراج من المستودع
- **تفاصيل الطلب**: عرض معلومات مفصلة عن كل طلب (اللون، الأبعاد، الطبخة، إلخ)
- **عناصر الطلب**: عرض جميع عناصر الطلب مع حالة كل عنصر

### 2. لوحة الأرقام (الجزء العلوي الأيمن)
- **حقول الإدخال**: الطول، العرض، السماكة، الوجهة
- **لوحة رقمية**: أزرار رقمية لإدخال القيم بسهولة
- **أزرار التحكم**: مسح، حذف آخر رقم، تأكيد الإخراج

### 3. جدول المخرجات (الجزء السفلي الأيمن)
- **عرض الحركات**: جدول يعرض جميع حركات المستودع
- **تفاصيل الحركة**: اللون، الأبعاد، الوجهة، التاريخ
- **تحديث تلقائي**: إعادة تحميل البيانات عند إنشاء حركة جديدة

## كيفية الاستخدام

### 1. اختيار طلب للإخراج
1. في قسم المدخلات، انقر على أي طلب لعرض تفاصيله
2. راجع معلومات الطلب وعناصره
3. حدد البيانات المطلوب إخراجها

### 2. إدخال البيانات عبر لوحة الأرقام
1. انقر على حقل الإدخال المطلوب (الطول، العرض، السماكة)
2. استخدم لوحة الأرقام لإدخال القيم
3. اختر الوجهة من القائمة المنسدلة

### 3. تأكيد الإخراج
1. تأكد من ملء جميع الحقول المطلوبة
2. انقر على زر "إخراج" الأخضر
3. ستظهر الحركة الجديدة في جدول المخرجات

## الواجهات البرمجية (APIs)

### جلب حركات المستودع
```
GET /api/warehouse-movement
Authorization: Bearer <token>
```

### إنشاء حركة مستودع جديدة
```
POST /api/warehouse-movement
Authorization: Bearer <token>
Content-Type: application/json

{
  "color_id": 1,
  "batch_id": 1,
  "length": 300,
  "width": 66,
  "thickness": 0.6,
  "destination": "slitting",
  "notes": "ملاحظات اختيارية"
}
```

### تحديث حركة مستودع
```
PUT /api/warehouse-movement/{id}
Authorization: Bearer <token>
Content-Type: application/json

{
  "length": 400,
  "destination": "cutting",
  "notes": "تحديث الملاحظات"
}
```

### حذف حركات مستودع
```
DELETE /api/warehouse-movement/all
Authorization: Bearer <token>
Content-Type: application/json

{
  "ids": [1, 2, 3]
}
```

## الأدوار والصلاحيات
- **Warehouse_Keeper**: الدور الوحيد المسموح له بالوصول للصفحة
- يتم التحقق من الدور عند محاولة الوصول للصفحة
- في حالة عدم وجود الصلاحية، يتم إعادة التوجيه للصفحة الرئيسية

## ملاحظات تقنية
- الصفحة تستخدم React Hooks لإدارة الحالة
- تدعم اللغة العربية مع اتجاه RTL
- تستخدم Tailwind CSS للتصميم
- تدعم الاستجابة للشاشات المختلفة
- تتحقق من صحة البيانات قبل الإرسال